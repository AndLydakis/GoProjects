package main
/*

import (
	"math"
	"fmt"
)

type Circle struct {
	x, y, r float64
}

type Rectange struct {
	x1, y1, x2, y2 float64
}

func mDistance(x1, y1, x2, y2 float64) float64 {
	a := x2 - x1
	b := y2 - y1
	return math.Sqrt(a*a + b*b)
}

func (r *Rectange) area() float64 {
	l := mDistance(r.x1, r.y1, r.x1, r.y2)
	w := mDistance(r.x1, r.y1, r.x2, r.y1)
	return l * w
}

func (c *Circle) area() float64 {
	return math.Pi * c.r * c.r
}

func CircleArea(c Circle) float64 {
	return c.r * c.r * math.Pi
}
func main() {
	// var c Circle
	// c:= new(Circle)
	c := Circle{0, 0, 5}
	r := Rectange{0, 0, 10, 10}
	fmt.Println(c.area())
	fmt.Println(r.area())
}
*/
